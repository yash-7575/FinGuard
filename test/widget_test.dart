import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should launch', (WidgetTester tester) async {
    // Basic smoke test - app can be imported
    expect(true, isTrue);
  });
}
